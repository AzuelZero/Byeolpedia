name: byeolpedia_frontend
description: "Byeolpedia - Kpop Tracker MVP"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1

  cupertino_icons: ^1.0.8
  
  dio: ^5.6.0
  
  provider: ^6.1.2
  
  json_annotation: ^4.9.0
  
  shared_preferences: ^2.3.2
  
  go_router: ^14.2.7

  cached_network_image: ^3.3.1
  
  tflite_flutter: ^0.10.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  
  json_serializable: ^6.8.0
  build_runner: ^2.4.12

flutter:
  uses-material-design: true